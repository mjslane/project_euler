version: '3.4'

services:
  app:
    image: ${DOCKER_REGISTRY-}app
    build:
      context: .
      dockerfile: project_euler/Dockerfile

  apptests:
    image: ${DOCKER_REGISTRY-}apptests
    build:
      context: .
      dockerfile: Dockerfile.test

